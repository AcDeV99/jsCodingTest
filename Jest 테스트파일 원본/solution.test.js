function solution() {}

describe('', () => {
  test('', () => {
    expect(solution()).toBe();
  });
  test('', () => {
    expect(solution()).toBe();
  });
  test('', () => {
    expect(solution()).toBe();
  });
});
